[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m montenegro_res_news.web"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
